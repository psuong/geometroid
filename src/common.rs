/// Some common settings for the engine because I'm starting with const settings :)

/// Standard 800 pixel width for the window.
pub const WIDTH: u32 = 800;

// Standard 600 pixel height for the window.
pub const HEIGHT: u32 = 600;

/// How many frames can we have in the buffer at any point in time?
pub const MAX_FRAMES_IN_FLIGHT: u32 = 2;
